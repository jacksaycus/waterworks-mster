spring:
  datasource:
    #driver-class-name: org.mariadb.jdbc.Driver
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    #url: jdbc:mariadb://10.15.32.150:13306/waio?autoReconnect=true&useSSL=false&rewriteBatchedStatements=true
    #url: jdbc:mariadb://211.58.232.114:27306/waio?autoReconnect=true&useSSL=false
    url: jdbc:mariadb://localhost:3306/waio?autoReconnect=true&useSSL=false&rewriteBatchedStatements=true
    username: iot
    password: iot.admin
    hikari:
      idle-timeout: 10000
      max-lifetime: 420000
      connection-timeout: 5000
      validation-timeout: 5000
#  data:
#    rest:
#      basePath: /api

mybatis:
  config-location: classpath:mybatis-config.xml
server:
  port: 8081

logging:
  level:
    org.kwater.waterworks: DEBUG
